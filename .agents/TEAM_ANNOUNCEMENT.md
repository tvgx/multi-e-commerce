# Team Announcement (mẫu)

Mẫu thông báo khi cập nhật `.agents/` hoặc quy trình:

```
📢 [Cập nhật .agents/] <tiêu đề>

Có gì mới: <tóm tắt 1–2 dòng>
Ảnh hưởng: <ai cần đọc / đổi thói quen gì>
Đọc tại: .agents/<đường-dẫn>
Hiệu lực: <ngày>
```

Kênh: #engineering-docs. Thay đổi rules → tăng `version` trong frontmatter (nếu có) + PR nhãn `docs`.
